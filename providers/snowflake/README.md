# Snowflake Provider candidate

This bounded candidate supplies canonical `memory` through Snowflake Data Cloud. Account, cloud, region, edition, workload role, compliance artifact, access-history and credit evidence remain explicit. Snowflake object and Cortex vocabulary never becomes Company Capability semantics, and `inference` is not advertised.

The adapter evaluates deterministic non-production fixtures only. Live credentials were unavailable, so the candidate cannot claim installation, health, live acceptance or governed/current status.
